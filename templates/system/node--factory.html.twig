{# My Custom content type template#}
{# <p> {{ node.body}} </p> #}
{# {{ node.body.value }} #}

{{ attach_library('custom_theme/messages') }}
{{ attach_library('custom_theme/external') }}


{# <span class="fida">{{ node.field_image_gellery.getvalue|length }}</span> #}

{# <img src="{{ file_url(node.field_image_gellery.entity.fileuri) }}" class="img-fluid" /> #} 

<p class="title">{{ node.title.value }}</p>
<div class="saad-body">{{ content.body.0 }}</div>

{{ file_url(node.field_factory_image['#items'].entity.uri.value) }}

<img src="{{ file_url(node.field_factory_image.entity.fileuri) }}" class="img-fluid" />


  <h2>My Gellery</h2>  
  <div class="container">
  <div id="myCarousel" class="carousel slide" data-ride="carousel">
    <!-- Indicators -->
    <ol class="carousel-indicators">
      {# 
      <li data-target="#myCarousel" data-slide-to="0" class="active"></li>   
      <li data-target="#myCarousel" data-slide-to="1"></li>
      <li data-target="#myCarousel" data-slide-to="2"></li>
       #}
       {% for row in node[0]['#rows'] %}
    <li data-target="#myCarousel" data-slide-to= {{ loop.index0 }} class={{ loop.first ? 'active' }}>
    </li>
      {% endfor %}
    </ol>
   
    <!-- Wrapper for slides -->
    <div class="carousel-inner">

      <div class="item active">
        <img src="{{ file_url(node.field_factory_image.entity.fileuri) }}" alt="Los Angeles" style="width:100%;">
      </div> 

      {% for image in node.field_image_gellery %}
        <div class="item">
          <img src="{{ file_url(image.entity.fileuri) }}" alt="Los Angeles" style="width:100%;"/>
        </div>
      {% endfor %}

      {# <div class="item">
        <img src="{{ file_url(node.field_factory_image.entity.fileuri) }}" alt="Chicago" style="width:100%;">
      </div>
    
      <div class="item">
        <img src="{{ file_url(node.field_factory_image.entity.fileuri) }}" alt="New york" style="width:100%;">
      </div> #}
    </div>

    <!-- Left and right controls -->
    <a class="left carousel-control" href="#myCarousel" data-slide="prev">
      <span class="glyphicon glyphicon-chevron-left"></span>
      <span class="sr-only">Previous</span>
    </a>
    <a class="right carousel-control" href="#myCarousel" data-slide="next">
      <span class="glyphicon glyphicon-chevron-right"></span>
      <span class="sr-only">Next</span>
    </a>
  </div>
  </div>
